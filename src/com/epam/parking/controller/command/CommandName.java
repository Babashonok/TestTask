package com.epam.parking.controller.command;

/**
 * enum for any command,which user can call
 */
public enum CommandName {
    ADDSLOT, ADDVEHICLE, OCCUPY, FREESLOT, STAT, DISCOUNT, WRONG_REQUEST
};
